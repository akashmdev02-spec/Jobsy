# Database — reads from Railway env vars
spring.datasource.url=jdbc:mysql://${mysql.railway.internal}:${3306}/${railway}
spring.datasource.username=${root}
spring.datasource.password=${ofjrHpoNljJwjmVMKDzoqQEYEnahKIpx}

# JWT
app.jwt.secret=${7a5adf24878cfa96e4bedf94ef4df049b42e09144eeef230fd271873d7d37db7}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Admin
app.admin.email=${ADMIN_EMAIL:admin@jobportal.com}
app.admin.password=${ADMIN_PASSWORD}
app.admin.name=${ADMIN_NAME:Admin}

# CORS — your Vercel frontend URL
app.cors.allowed-origins=${CORS_ORIGINS:https://jobsy-dun.vercel.app/}

# Uploads
app.upload.dir=${UPLOAD_DIR:./uploads/resumes}
